registry:
  property: Registry Profile Dev

#Application Configuration
server:
  error:
    include-message: always
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
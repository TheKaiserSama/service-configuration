registry:
  property: Registry Profile Dev

spring:
  output:
    ansi:
      enabled: always

server:
  error:
    include-message: always
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false